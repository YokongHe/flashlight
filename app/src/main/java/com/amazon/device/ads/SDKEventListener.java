package com.amazon.device.ads;

import com.amazon.device.ads.AdControlAccessor;
import com.amazon.device.ads.SDKEvent;

interface SDKEventListener {
   void onSDKEvent(SDKEvent var1, AdControlAccessor var2);
}
